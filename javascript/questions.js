// write a code to generate a number between 0 and 9 (included)
// let num = Math.floor(Math.random()*10);
// console.log(num);
// h.w
//write a code to generate a random integer between 0 and 10 (included)
//write a code to generate a random integer between -10 and 10 (included)

if (Math.random()<0.5){
    console.log('hello');
}
else
    console.log("hello ji")
